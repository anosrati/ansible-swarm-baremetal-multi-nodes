OP:  GET  http://<ipAddr>/redfish/v1/
     GET  http://<ipAddr>/redfish/v1

EX:  curl http://192.168.19.3/redfish/v1/
EX:  curl http://192.168.19.3/redfish/v1

Returns the service root.

Currently this does NOT have:
  -- the EventService
 
